# Руководство по использованию системы локализации

## 📁 Структура файлов

### `/app/assets/strings.js`
Основной файл со всеми переводами для трех языков (ru, en, uz)

### `/app/hooks/useStrings.js`
Хук для использования переводов в компонентах

## 🚀 Как использовать

### 1. В любом компоненте:
```javascript
import { useStrings } from "@/app/hooks/useStrings";

export default function MyComponent() {
    const { t, language } = useStrings();
    
    return (
        <div>
            <h1>{t('settings.title')}</h1>
            <p>{t('projects.description')}</p>
        </div>
    );
}
```

### 2. Изменение языка:
```javascript
import { changeLanguage } from "@/app/hooks/useStrings";

// Изменить язык на английский
changeLanguage('en');

// Изменить язык на узбекский  
changeLanguage('uz');

// Изменить язык на русский
changeLanguage('ru');
```

### 3. Структура ключей в strings.js:
- `t('nav.projects')` → навигация
- `t('settings.title')` → настройки  
- `t('projects.description')` → проекты
- `t('notes.addNote')` → заметки
- `t('map.title')` → карта
- `t('location.addLocation')` → местоположения

## ✨ Особенности

- **Автоматическое сохранение** выбранного языка в localStorage
- **Реактивное обновление** всех компонентов при смене языка
- **Fallback на русский** если перевод не найден
- **Поддержка вложенных ключей** (например: 'settings.profile.name')

## 🔧 Что уже переведено

✅ Сайдбар (навигация)
✅ Страница настроек
✅ Страница проектов (частично)
✅ Модальные окна (частично)

## 📝 Как добавить новые переводы

1. Добавьте ключ во все три языка в `strings.js`
2. Используйте `t('your.new.key')` в компоненте
3. Язык автоматически подтянется из настроек пользователя

Пример добавления нового перевода:
```javascript
// В strings.js
ru: {
    mySection: {
        newButton: "Новая кнопка"
    }
},
en: {
    mySection: {
        newButton: "New Button" 
    }
},
uz: {
    mySection: {
        newButton: "Yangi tugma"
    }
}

// В компоненте
{t('mySection.newButton')}
```