# Обновления проекта GeoNote

## Основные изменения

### ✅ Новая организационная структура базы данных

Проект полностью переработан для поддержки многопользовательской работы через организации.

### 📁 Обновленные файлы

#### Компоненты:
- ✅ `app/components/CreateMeetingModal.js` - Добавлено поле организации (только для чтения), обновлена структура данных
- ✅ `app/components/MeetingCard.js` - Обновлено для работы с полем `date` вместо `datetime`
- ✅ `app/components/MeetingListItem.js` - Полностью переработан для отображения встреч с новой структурой
- ✅ `app/components/ProjectCardforHS.js` - Совместим с новой структурой (изменений не требуется)

#### Страницы:
- ✅ `app/pages/homeScreen/page.js` - Добавлена логика получения организации пользователя
- ✅ `app/pages/projects/page.js` - Обновлена для работы с организационной структурой
- ✅ `app/pages/meetings/page.js` - Обновлена для работы с организационной структурой
- ✅ `app/pages/library/page.js` - Добавлена поддержка организаций

### 🔄 Ключевые изменения в структуре данных

#### Meetings (Встречи)
```javascript
{
  title: string,
  date: timestamp,           // ← изменено с 'datetime'
  location: string,
  owner: userId,            // ← организатор встречи
  projectId: string | null, // ← опционально
  participants: [userId],
  notes: string,
  createdAt: timestamp,
  updatedAt: timestamp
}
```

#### Projects (Проекты)
```javascript
{
  title: string,
  description: string,
  createdBy: userId,        // ← изменено с 'owner'
  members: [userId],        // ← изменено с 'participants'
  status: string,
  startDate: timestamp,
  endDate: timestamp,
  createdAt: timestamp
}
```

#### Users in Organization (Пользователи в организации)
```javascript
{
  name: string,
  email: string,
  role: string,             // "admin", "member", etc.
  joinedAt: timestamp
}
```

### 🔐 Важные правила

1. **Организация нельзя изменять** при создании встречи или проекта
   - Организация определяется автоматически из `users/{userId}.organizationId`
   - Поле отображается как disabled (только для чтения)

2. **Все данные изолированы по организациям**
   - Каждая организация имеет свои подколлекции: users, projects, meetings
   - Пользователи видят только данные своей организации

3. **Фильтрация данных**
   - Проекты: показываются только те, где пользователь - создатель или участник
   - Встречи: показываются только те, где пользователь - организатор или участник

### 📋 Что нужно сделать перед запуском

1. **Мигрировать данные** (см. `DATABASE_MIGRATION.md`)
   - Создать организацию
   - Переместить пользователей в подколлекцию организации
   - Переместить проекты и встречи в подколлекции организации
   - Обновить документы пользователей (добавить `organizationId`)

2. **Обновить правила безопасности Firestore** (см. `DATABASE_MIGRATION.md`)

3. **Убедиться, что все пользователи имеют организацию**
   ```javascript
   users/{userId} {
     organizationId: "org-id-here"
   }
   ```

### 🎯 Новый функционал

#### CreateMeetingModal
- Отображение организации (только для чтения)
- Выбор проекта из списка проектов организации
- Выбор участников из пользователей организации
- Отображение ролей пользователей
- Улучшенная валидация формы

#### Страницы
- Автоматическое определение организации пользователя
- Загрузка данных только для текущей организации
- Отображение названия организации
- Обработка случаев, когда пользователь не привязан к организации

### 🚀 Как использовать

```javascript
// Пример использования CreateMeetingModal
<CreateMeetingModal 
  isOpen={isModalOpen}
  onClose={() => setIsModalOpen(false)}
  onSuccess={handleSuccess}
  orgId={currentOrgId}      // обязательно
  projectId={projectId}     // опционально
/>
```

### 📝 Структура путей в Firestore

```
/organizations/{orgId}/users/{userId}
/organizations/{orgId}/projects/{projectId}
/organizations/{orgId}/meetings/{meetingId}
/users/{userId}
```

### ⚠️ Важные примечания

1. Все компоненты теперь требуют `orgId` как prop
2. `Sidebar` и `Navbar` должны получать `orgId`
3. Перед использованием приложения убедитесь, что все пользователи имеют `organizationId`
4. Данные изолированы - пользователи разных организаций не видят данные друг друга

### 🐛 Известные ограничения

- Пользователь может быть только в одной организации
- Нельзя переключаться между организациями
- Нет функционала создания новых организаций из UI (требуется Firebase Admin)

### 📚 Дополнительная документация

- См. `DATABASE_MIGRATION.md` для деталей миграции
- Проверьте правила безопасности Firestore перед развертыванием
